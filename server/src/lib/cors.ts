// ---------------------------------------------------------------
// بيرجع قايمة الـ origins المسموح لهم — نفس المنطق بيتستخدم في
// الـ Express CORS وفي Socket.io عشان الاتنين يتفقوا مع بعض
// ---------------------------------------------------------------
// القراءة والتحقق بيحصلوا في lib/config.ts وقت تشغيل السيرفر — مش هنا.
// قبل كده الدالة دي كانت بتعمل fallback لـ "http://localhost:5173" لو
// CLIENT_URL ناقص، فالسيرفر كان بيقوم في الإنتاج بإعداد غلط من غير ما حد ياخد
// باله. دلوقتي config بيرفض ده وقت التشغيل.
import { config } from "./config.js";

export function getAllowedOrigins(): string[] {
  return config.allowedOrigins;
}
